/*
    Script: create-database.sql
    Autor: Vitor Pratt
    Projeto: All In Store Analytics
    Descrição:
    Este script cria o banco de dados principal utilizado no projeto.
    Execute este arquivo antes dos scripts de criação de tabelas.
*/

-- Criar o banco de dados (caso não exista)
IF NOT EXISTS (SELECT * FROM sys.databases WHERE name = 'AllInStore')
BEGIN
    CREATE DATABASE AllInStore;
END
GO

-- Seleciona o banco para uso
USE AllInStore;
GO
